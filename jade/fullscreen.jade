extends _layout

block content
	- var section = 'fullscreen'

	.grid
		.col-6
			include ./partials/leftcol.jade

		.col-18
			h1 Fullscreen

			+breadcrumbs([
				['components', 'Components'],
				['fullscreen', 'Fullscreen']
			])

			h2 Fullscreen demo
			p The full screen have a rich API, so you can tune it how you prefer

			p: a.button#fullscreen-demo(href="#") Open fullscreen

			.grid.marginless
				include partials/code